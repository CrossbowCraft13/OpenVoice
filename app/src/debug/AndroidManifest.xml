<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only overlay: allows cleartext HTTP to loopback only, so instrumented
    tests can exercise ModelManager.downloadModel against a local test server.
    Release builds are unaffected (they keep the platform default: no cleartext).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
